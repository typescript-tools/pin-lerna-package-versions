import { testProp, fc } from 'ava-fast-check'

/**
 * Library under test
 */

import { pinLernaPackageVersions } from '../../src/pin-lerna-package-versions'

testProp.skip(
    'TODO: property-test pin-lerna-package-versions',
    [
        // arbitraries
    ],
    (
        // test arguments
    ) => {
        // return assertion
    }, {
        verbose: true,
        numRuns: 100
    }
)
